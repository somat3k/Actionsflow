# ── Core data & ML ────────────────────────────────────────────────────────────
numpy>=1.24,<2.0
pandas>=2.0,<3.0
scikit-learn>=1.3,<2.0
xgboost>=2.0,<3.0
joblib>=1.3
tqdm>=4.65

# ── Deep learning (optional – LSTM model) ─────────────────────────────────────
# Install separately if needed:
# tensorflow>=2.14,<3.0   # or tensorflow-cpu

# ── ONNX model export (optional – export-models run-type) ─────────────────────
# Install separately if needed:
# skl2onnx>=1.16,<2.0
# onnxruntime>=1.17,<2.0

# ── Dataset caching (optional) ────────────────────────────────────────────────
# safetensors>=0.4.2,<1.0

# ── Google Gemini AI ──────────────────────────────────────────────────────────
google-generativeai>=0.5,<1.0

# ── Hyperliquid / Web3 ────────────────────────────────────────────────────────
requests>=2.31,<3.0
eth-account>=0.10,<1.0
websocket-client>=1.7,<2.0

# ── Configuration & utilities ─────────────────────────────────────────────────
PyYAML>=6.0,<7.0
python-dotenv>=1.0,<2.0

# ── Cache (Redis + embedded fakeredis) ────────────────────────────────────────
# fakeredis provides an embedded in-process Redis instance (no external daemon).
# Install the redis extras to enable: pip install "quantum-trader[redis]"
redis>=5.0,<8.0
fakeredis>=2.20,<3.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.32,<2.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4,<9.0
pytest-mock>=3.12,<4.0
